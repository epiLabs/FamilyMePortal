# Adapt this sample to your database configuration
# and then rename it into database.yml
# See http://stackoverflow.com/questions/2856996/git-and-rails-ignore-database-yml

# If you want to use Postgres:
development:
  host: localhost
  adapter: postgresql
  encoding: unicode
  database: familyMe_development
  pool: 5
  min_messages: WARNING

# If you want to use Mysql
# development:
#   adapter: mysql2
#   encoding: utf8
#   database: my_db_name
#   username: root
#   password: my_password
#   host: 127.0.0.1
#   port: 3306

#PostgreSQL Developement server - commons/shared
# development:
#   host: frlbs.net
#   adapter: postgresql
#   encoding: unicode
#   username: familyme
#   password: 7JJueRnxrxsNYtWN
#   database: familymedev
#   pool: 5

production:
  host: localhost
  adapter: postgresql
  encoding: unicode
  database: familyMe_production
  pool: 5

# MySQL Production server
# production:
#   adapter: mysql2
#   encoding: utf8
#   database: familyMe
#   username: familyMe
#   password: 7JJueRnxrxsNYtWN
#   host: frlbs.net
#   port: 3306

# PostgreSQL Production server
# production:
#   host: frlbs.net
#   adapter: postgresql
#   encoding: unicode
#   username: familyme
#   password: 7JJueRnxrxsNYtWN
#   database: familyme
#   pool: 5

  # Connect on a TCP socket. Omitted by default since the client uses a
  # domain socket that doesn't need configuration. Windows does not have
  # domain sockets, so uncomment these lines.
  #host: localhost
  #port: 5432

  # Schema search path. The server defaults to $user,public
  #schema_search_path: myapp,sharedapp,public

  # Minimum log levels, in increasing order:
  #   debug5, debug4, debug3, debug2, debug1,
  #   log, notice, warning, error, fatal, and panic
  # The server defaults to notice.
  #min_messages: warning

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.

test: &test
  adapter: sqlite3
  database: db/test.sqlite3
  timeout: 5000

cucumber:
  <<: *test
